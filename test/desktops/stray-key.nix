# A top-level key with no tailored message: the generic half of the closed
# shape, which is what catches whatever nix-darwin option nobody thought of.
{
  launchd.user.agents.evil = { };
  haus.ui.scale = 1.2;
}
