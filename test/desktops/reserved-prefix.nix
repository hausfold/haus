# A private room, named by a desktop. `haus.my.*` is the prefix haus reserves
# for rooms that live on one Mac — which is exactly what a shared desktop may
# not name, since whoever runs it does not have that room.
{
  haus.my.photography.enable = true;
}
