# A scene key with a space in it. The key is what a person types after
# `focus scene` and what the engine looks up in a JSON table, so it has to be
# one plain word — the same rule roster ids and workspace names already carry.
{
  haus.focus.scenes."deep work".dnd = true;
}
