diff --git a/internal/tui/components/footer/footer.go b/internal/tui/components/footer/footer.go
index f8ca6a0..26b969b 100644
--- a/internal/tui/components/footer/footer.go
+++ b/internal/tui/components/footer/footer.go
@@ -8,11 +8,9 @@ import (
 	bbHelp "charm.land/bubbles/v2/help"
 	"charm.land/lipgloss/v2"
 	"charm.land/lipgloss/v2/compat"
-	zone "github.com/lrstanley/bubblezone/v2"
 
 	"github.com/dlvhdr/gh-dash/v4/internal/config"
 	"github.com/dlvhdr/gh-dash/v4/internal/git"
-	"github.com/dlvhdr/gh-dash/v4/internal/tui/constants"
 	"github.com/dlvhdr/gh-dash/v4/internal/tui/context"
 	"github.com/dlvhdr/gh-dash/v4/internal/tui/keys"
 	"github.com/dlvhdr/gh-dash/v4/internal/utils"
@@ -50,18 +48,12 @@ func (m Model) View() string {
 		footer = lipgloss.NewStyle().
 			Render("Really quit? (Press y/enter to confirm, any other key to cancel)")
 	} else {
-		helpIndicator := lipgloss.NewStyle().
-			Background(m.ctx.Theme.FaintText).
-			Foreground(m.ctx.Theme.SelectedBackground).
-			Padding(0, 1).
-			Render("? help")
-		donationIndicator := zone.Mark("donate", lipgloss.NewStyle().
-			Background(m.ctx.Theme.SelectedBackground).
-			Foreground(m.ctx.Theme.WarningText).
-			Padding(0, 1).
-			Underline(true).
-			Render(fmt.Sprintf("%s donate", constants.DonateIcon)))
-		viewSwitcher := m.renderViewSwitcher(m.ctx)
+		// nebelhaus: the footer BAR is gone — the view switcher, repo/user
+		// pills, the donate link and the help hint were chrome, and the hint
+		// now lives in the header's right slot (tabs.go viewHelpHint). The row
+		// itself survives, unstyled, because it is also where prompt
+		// confirmations, the pager and running-task status are drawn; blanking
+		// it entirely would hide a y/N prompt that still eats the keypress.
 		leftSection := ""
 		if m.leftSection != nil {
 			leftSection = *m.leftSection
@@ -70,24 +62,15 @@ func (m Model) View() string {
 		if m.rightSection != nil {
 			rightSection = *m.rightSection
 		}
-		spacing := lipgloss.NewStyle().
-			Background(m.ctx.Theme.SelectedBackground).
-			Render(
-				strings.Repeat(
-					" ",
-					utils.Max(0,
-						m.ctx.ScreenWidth-lipgloss.Width(
-							viewSwitcher,
-						)-lipgloss.Width(leftSection)-
-							lipgloss.Width(rightSection)-
-							lipgloss.Width(
-								helpIndicator,
-							)-lipgloss.Width(donationIndicator),
-					)))
-
-		footer = m.ctx.Styles.Common.FooterStyle.
-			Render(lipgloss.JoinHorizontal(lipgloss.Top, viewSwitcher, leftSection, spacing,
-				rightSection, donationIndicator, helpIndicator))
+		spacing := strings.Repeat(
+			" ",
+			utils.Max(0,
+				m.ctx.ScreenWidth-lipgloss.Width(leftSection)-
+					lipgloss.Width(rightSection),
+			))
+
+		footer = lipgloss.JoinHorizontal(
+			lipgloss.Top, leftSection, spacing, rightSection)
 	}
 
 	if m.ShowAll {
diff --git a/internal/tui/components/tabs/tabs.go b/internal/tui/components/tabs/tabs.go
index b1f66cf..095941d 100644
--- a/internal/tui/components/tabs/tabs.go
+++ b/internal/tui/components/tabs/tabs.go
@@ -69,7 +69,7 @@ func (m Model) Update(msg tea.Msg) (Model, tea.Cmd) {
 
 func (m Model) View() string {
 	c := m.carousel.View()
-	logo := m.viewLogo()
+	logo := m.viewHelpHint()
 	return m.ctx.Styles.Tabs.TabsRow.
 		Width(m.ctx.ScreenWidth).
 		Height(common.HeaderHeight).
@@ -110,7 +110,7 @@ func (m *Model) UpdateProgramContext(ctx *context.ProgramContext) {
 		Separator:         ctx.Styles.Tabs.TabSeparator,
 	})
 
-	m.carousel.SetWidth(ctx.ScreenWidth - lipgloss.Width(m.viewLogo()))
+	m.carousel.SetWidth(ctx.ScreenWidth - lipgloss.Width(m.viewHelpHint()))
 }
 
 func (m *Model) SetSections(sections []section.Section) {
@@ -150,28 +150,21 @@ func (m *Model) UpdateTabTitles() {
 	m.carousel.SetCursor(oldCursor)
 }
 
-func (m *Model) viewLogo() string {
-	version := lipgloss.NewStyle().Foreground(m.ctx.Theme.SecondaryText).Render(m.ctx.Version)
-	if m.latestVersion != "" && m.ctx.Version != "dev" && m.ctx.Version != m.latestVersion {
-		version = lipgloss.JoinVertical(
-			lipgloss.Left,
-			version,
-			lipgloss.NewStyle().
-				Foreground(m.ctx.Styles.Colors.SuccessText).
-				Render(" Update available!"),
-		)
-	} else {
-		version = lipgloss.PlaceVertical(2, lipgloss.Bottom, version)
-	}
+// nebelhaus: the header's right slot used to hold the gh-dash logo and the
+// version string. Neither earns its width in a full-window overlay, so the
+// slot carries the "? help" hint instead — the one thing worth keeping from
+// the footer bar, which this rice blanks out (see footer.go).
+func (m *Model) viewHelpHint() string {
+	hint := lipgloss.NewStyle().
+		Background(m.ctx.Theme.FaintText).
+		Foreground(m.ctx.Theme.SelectedBackground).
+		Padding(0, 1).
+		Render("? help")
 
 	return lipgloss.NewStyle().
 		Padding(0, 1, 0, 2).
 		Height(2).
-		Render(lipgloss.JoinHorizontal(lipgloss.Bottom,
-			lipgloss.NewStyle().Foreground(context.LogoColor).Render(constants.Logo),
-			" ",
-			version,
-		))
+		Render(lipgloss.PlaceVertical(2, lipgloss.Bottom, hint))
 }
 
 func (m *Model) SetAllLoading() []tea.Cmd {
