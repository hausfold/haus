# A package-typed leaf. Unreachable from data anyway — this is the option that
# has a `packageName` sibling precisely so a shared file can name it instead.
{
  haus.fonts.mono.package = "berkeley-mono";
}
